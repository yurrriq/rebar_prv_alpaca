{erl_opts, [
  {platform_define, "^(19|2)", rand_only},
  no_debug_info,
  warnings_as_errors
]}.

{deps, [lfe]}.

{profiles, [
  {test, [
    {deps, [{meck, "0.8.4"}]},
    {erl_opts, [debug_info]}
  ]}
]}.
